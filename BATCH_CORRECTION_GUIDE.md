# ==============================================================================
# FACS批次效应分析完整指南
# ==============================================================================
#
# 实验设计：
#   - BatchA (同天): BM + Culture (都有GFP)
#   - BatchC (另天): Sabq (有GFP)
#   - 对照：两个批次都有未染色BM样本（来自不同老鼠）
#
# 目标：比较三组的PE+GFP+细胞MFI
#
# 挑战：批次与组完全混淆
# ==============================================================================

## 分析流程（3步骤）

### 步骤1：批次效应检测 ⭐⭐⭐

**目的**：确定是否需要批次矫正

**方法**：使用未染色BM样本
```r
source("detect_batch_effects_controls.R")
```

**判断标准**：
- p < 0.01 且差异 > 10% → **必须**矫正
- p < 0.05 且差异 > 5-10% → **建议**矫正
- p >= 0.05 或差异 < 5% → **可能不需要**矫正

**输出解读**：
```
GFP通道批次差异检验：
  BatchA中位数: 450 (未染色的自发荧光)
  BatchC中位数: 500
  差异: 50 (11.1%)  ← 如果 > 10%，需要矫正
  p值: 0.001        ← 如果 < 0.01，差异显著
```

---

### 步骤2：批次矫正策略选择

根据步骤1的结果，选择合适的方法：

#### 方案A：简单校正法（推荐，如果批次效应简单）

**适用情况**：
- 批次效应在各通道相似
- 主要是整体信号偏移

**原理**：
```
批次效应 = BatchC未染色中位数 - BatchA未染色中位数
矫正后的Sabq = 原始Sabq - 批次效应
```

**优点**：
- ✅ 简单直接
- ✅ 容易理解和解释
- ✅ 不会过度矫正

**缺点**：
- ⚠️ 假设批次效应在所有强度范围相同（可能不成立）

**实现**：见下面的 `simple_batch_correction.R`

---

#### 方案B：cyCombine矫正（更复杂，如果批次效应复杂）

**适用情况**：
- 批次效应在不同强度范围不同
- 需要非线性矫正

**使用哪个cyCombine方法？**

根据官方教程，cyCombine有两种主要使用场景：

##### 场景1：单panel，多批次（你的情况）
**教程**：https://biosurf.org/cyCombine_CyTOF_1panel.html

**要求**：
- ❌ 需要：同样的细胞类型在多个批次中都存在
- ❌ 你的情况：每个组只在一个批次（不满足）

**结论**：**不适合直接使用**

##### 场景2：使用对照样本引导的矫正（改进方案）

**策略**：
```
1. 用未染色样本估计批次效应的"模式"
2. 将这个模式应用到PE+GFP+细胞
```

**修改的cyCombine流程**：
1. 提取未染色样本 + PE+GFP+细胞
2. 将未染色标记为"reference"
3. 运行batch_correct，covar=NULL
4. 仅使用PE+GFP+细胞的矫正值

**优点**：
- ✅ 可以处理非线性批次效应
- ✅ 使用了对照样本的信息

**缺点**：
- ⚠️ 仍然假设未染色和染色细胞的批次效应模式相似
- ⚠️ 更复杂，容易出错

**实现**：见下面的 `cycombine_batch_correction.R`

---

#### 方案C：不矫正，统计模型控制（保守方案）

**适用情况**：
- 批次效应不显著（p >= 0.05）
- 或差异很小（< 5%）

**方法**：
```r
# 线性模型，batch作为协变量
lm(GFP_MFI ~ group + batch, data = sample_summary)
```

**优点**：
- ✅ 最保守
- ✅ 在统计模型中明确控制批次

**缺点**：
- ❌ 由于批次与组完全混淆，batch效应无法与group分离
- ❌ 模型会给出警告：colinearity

**结论**：**不推荐**（因为批次与组混淆）

---

### 步骤3：验证矫正效果

**如果进行了矫正**，需要验证：

1. **未染色样本的批次差异是否消除**
   ```r
   # 矫正后，BatchA未染色 vs BatchC未染色应该相似
   ```

2. **生物学差异是否保留**
   ```r
   # BatchA内部：BM vs Culture的差异应该保留
   ```

3. **可视化检查**
   ```r
   # 密度图：矫正后批次应该重叠
   # 但组间差异应该仍然可见
   ```

---

## 推荐的完整流程

### 流程图

```
开始
  ↓
[1] 运行批次效应检测
  ↓
是否有显著批次效应？
  ├─ 否 (p>=0.05, 差异<5%)
  │   → 直接比较三组MFI
  │   → 在论文中说明已检测，无显著批次效应
  │
  └─ 是 (p<0.05, 差异>=5%)
      ↓
    差异大小？
      ├─ 小到中等 (5-15%)
      │   → 使用简单校正法（方案A）⭐推荐
      │   → 验证矫正效果
      │
      └─ 大 (>15%)
          → 考虑cyCombine（方案B）
          → 或重新设计实验
```

---

## 关键建议

### ✅ 推荐：方案A（简单校正法）

**理由**：
1. 你有完美的对照样本（两批次的未染色BM）
2. 简单直接，容易验证
3. 不会引入过度矫正的风险
4. 在论文中容易解释

**步骤**：
```r
# 1. 检测批次效应
source("detect_batch_effects_controls.R")

# 2. 如果有显著批次效应，进行简单校正
source("simple_batch_correction.R")

# 3. 比较三组MFI
source("compare_three_groups.R")
```

### ⚠️ 不推荐：直接用cyCombine矫正实验样本

**原因**：
- 批次与组完全混淆
- cyCombine无法区分批次效应和生物学差异
- 可能矫正掉真实的组间差异

### ✅ 可选：使用cyCombine，但仅在未染色样本上

**如果简单校正效果不好**，可以：
1. 用cyCombine估计未染色样本的批次效应模式
2. 手动应用到实验样本

---

## 论文报告建议

### 方法部分

**如果有批次效应，进行了校正**：
```
批次效应处理：
我们使用每个批次的未染色骨髓对照样本检测批次效应。
Wilcoxon检验显示GFP通道存在显著批次差异（p = 0.001，
中位数差异11%）。我们计算了两个批次未染色样本的中位数
差异，并将此差异从批次C的数据中减去，以消除技术性
批次效应。矫正后，两个批次的未染色样本荧光强度无
显著差异（p = 0.45），验证了矫正的有效性。
```

**如果无批次效应**：
```
批次效应检测：
我们使用每个批次的未染色骨髓对照样本检测批次效应。
Wilcoxon检验显示GFP通道无显著批次差异（p = 0.23，
中位数差异3.2%）。因此，我们直接比较了三组的荧光
强度，未进行批次矫正。
```

---

## 下一步行动

1. **立即运行**：
   ```r
   source("detect_batch_effects_controls.R")
   ```

2. **查看结果**，告诉我：
   - p值是多少？
   - 差异百分比是多少？

3. **我会根据结果**：
   - 创建对应的批次校正脚本
   - 帮你完成最终的三组比较

---

## 常见问题

**Q1: 未染色样本来自不同老鼠，会有问题吗？**

A: 如果是近交系（inbred strain）老鼠，基因相同，个体差异很小。
   差异主要来自批次效应（仪器、试剂、时间）。
   如果是远交系，可能有个体差异，但仍然比批次效应小。

**Q2: 简单校正会不会太简单？**

A: 不会。Occam's Razor原则：简单的方法往往更稳健。
   除非有证据表明批次效应是非线性的，否则简单校正足够。

**Q3: 如果批次效应很大（>20%）怎么办？**

A: 考虑重新设计实验：
   - 在BatchC也跑一些BM或Culture样本
   - 或在BatchA也跑一些Sabq样本
   这样可以完全区分批次效应和生物学差异。

**Q4: cyCombine的UMAP图为什么看起来奇怪？**

A: 因为批次与组混淆！cyCombine试图消除批次差异，
   但它分不清哪些是批次差异，哪些是真实的组间差异。
   结果就是把真实差异也矫正掉了，导致UMAP图异常。

---

## 参考资料

- cyCombine官方教程：https://biosurf.org/cyCombine_CyTOF_1panel.html
- 批次效应检测：https://biosurf.org/cyCombine_detect_batch_effects.html
- Nature Communications论文：doi:10.1038/s41467-022-29383-5
